<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Level Up Day Kiaaa ✨</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        html {
            scroll-behavior: smooth;
        }
        body {
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: #faf6f0;
            color: #4a3e3d;
            overflow-x: hidden;
        }

        /* Navigasi / Floating Music Button */
        .music-control {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.85);
            padding: 10px 20px;
            border-radius: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 600;
            font-size: 0.9em;
            transition: all 0.3s ease;
            border: 1px solid #e8d8ce;
        }
        .music-control:hover {
            transform: scale(1.05);
            background: #fff;
        }

        /* Styling Tiap Section */
        section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 80px 20px;
            text-align: center;
        }

        /* Section 1: Hero Pembuka */
        #hero {
            background: linear-gradient(180deg, #fff5f0 0%, #faf6f0 100%);
            position: relative;
        }
        .hero-content h1 {
            font-size: 3em;
            color: #b57c68;
            margin-bottom: 15px;
            animation: fadeInDown 1.5s ease;
        }
        .hero-content p {
            font-size: 1.2em;
            color: #8a7673;
            margin-bottom: 30px;
            animation: fadeInUp 1.5s ease;
        }
        .scroll-down {
            font-size: 0.9em;
            color: #b57c68;
            text-decoration: none;
            border-bottom: 2px solid #b57c68;
            padding-bottom: 4px;
            animation: bounce 2s infinite;
        }

        /* Section 2: Galeri Foto */
        #gallery {
            background-color: #f5ede4;
            width: 100%;
        }
        #gallery h2 {
            font-size: 2.2em;
            color: #b57c68;
            margin-bottom: 40px;
        }
        .grid-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            max-width: 1000px;
            width: 100%;
            padding: 0 20px;
        }
        .photo-card {
            background: white;
            padding: 12px;
            border-radius: 8px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.04);
            transition: transform 0.3s ease;
        }
        .photo-card:hover {
            transform: translateY(-5px) rotate(2deg);
        }
        .photo-card img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 4px;
        }
        .photo-card p {
            margin-top: 10px;
            font-style: italic;
            font-size: 0.9em;
            color: #7a6b68;
        }

        /* Section 3: Pesan & Musik */
        #message {
            background: linear-gradient(180deg, #f5ede4 0%, #fff5f0 100%);
            max-width: 800px;
            margin: 0 auto;
        }
        .letter-box {
            background: white;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.03);
            max-width: 600px;
            width: 100%;
            border: 1px solid #ede1d7;
        }
        .letter-box p {
            font-size: 1.1em;
            line-height: 1.8;
            margin-bottom: 20px;
            color: #5c4d4c;
        }
        .signature {
            font-weight: bold;
            font-size: 1.2em;
            color: #b57c68;
            text-align: right;
            margin-top: 30px;
        }

        /* Animasi */
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        @media (max-width: 600px) {
            .hero-content h1 { font-size: 2.2em; }
            .letter-box { padding: 25px; }
        }
    </style>
</head>
<body>

    <audio id="level-up" loop>
        <source src="nothing.mp3" type="audio/mpeg">
    </audio>

    <div class="music-control" onclick="toggleMusic()">
        <span id="music-icon">🎵</span>
        <span id="music-text">Putar Musik</span>
    </div>

    <section id="hero">
        <div class="hero-content">
            <h1>Selamat Ulang Tahun, Kiaa! 🎉</h1>
            <p>Hari ini seutuhnya milik lu. Scroll ke bawah ya...</p>
            <a href="#gallery" class="scroll-down">Lihat Momen Kita ↓</a>
        </div>
    </section>

    <section id="gallery">
        <h2>Our Beautiful Memories ✨</h2>
        <div class="grid-gallery">
            
            <div class="photo-card">
                <img src="kia3.png" alt="Momen 1">
                <p>Momen manis pertama kali ✨</p>
            </div>

            <div class="photo-card">
                <img src="kia1.png" alt="Momen 2">
                <p>Selalu seru kalau sama lu 🤍</p>
            </div>

            <div class="photo-card">
                <img src="kia2.png" alt="Momen 3">
                <p>Tawa yang paling gua suka ☀️</p>
            </div>

        </div>
    </section>

    <section id="message">
        <div class="letter-box">
            <p>"Semoga di umur yang baru ini, lu selalu dikelilingi kebahagiaan, kesehatan, dan hal-hal baik lainnya. Semua impian dan yang lagi lu semogakan, semoga bisa segera terwujud satu per satu."</p>
            <p>"Makasih ya udah selalu jadi orang yang luar biasa. Tetap semangat menjalani hari-harinya, dan jangan pernah lupa kalau lu berharga."</p>
            <div class="signature">Dari, Faren</div>
        </div>
    </section>

    <script>
        const music = document.getElementById('birthday-music');
        const musicText = document.getElementById('music-text');
        const musicIcon = document.getElementById('music-icon');

        function toggleMusic() {
            if (music.paused) {
                music.play().catch(error => {
                    console.log("Autoplay diblokir oleh browser, butuh interaksi user.");
                });
                musicText.innerText = "Matikan Musik";
                musicIcon.innerText = "⏸️";
            } else {
                music.pause();
                musicText.innerText = "Putar Musik";
                musicIcon.innerText = "🎵";
            }
        }
    </script>

</body>
</html>